---
description: Trinity Guard 三系统协同量化交易框架 — 项目全貌与系统宪法（博弈终版）
alwaysApply: true
---

# Trinity Guard — 系统宪法

## 第 1 章 系统哲学与顶层原则

### 1.1 定位
Trinity Guard 是一套面向中国 A 股的多 Agent 协同量化交易系统。三子系统并行博弈，目标不是"预测更准"，而是"防止同一种错误被集体放大"。

### 1.2 顶层铁律
系统只回答一个问题：**此刻能不能参与这场博弈？**
- 不回答"这只票理论上值不值得买"
- 识别今天是否值得参与 → 识别主战场是否可信 → 识别候选标的是否可交易

### 1.3 博弈驱动
- 交易系统最重要的不是找到信号，而是找到"别人会在什么时候犯错"
- 利用三种市场低效：情绪差 / 资金路径 / 信息扩散延迟
- 对手盘思维贯穿全流程

### 1.4 确定性与不确定性分离
- **确定性任务交给代码**：数据采集、技术指标计算、历史统计、ATR/MA/胜率
- **不确定性判断交给 LLM**：政策解读、叙事构建、反驳、审计、博弈态识别
- LLM 不允许自行计算数值指标，只允许解读代码引擎的计算结果

### 1.5 基础设施
- LLM 网关：one-api (Docker, 端口 3888)，路由到 DeepSeek / GPT / Claude / Gemini
- 数据源：Tushare（A 股核心数据）+ L1/L2 实时行情
- 运行模式：模拟盘优先（Paper Trading），验证通过后切实盘
- 券商适配：Broker Adapter 模式，支持本地模拟 / 同花顺 / 东方财富

---

## 第 2 章 三系统架构

### 2.1 认知异质性原则
三系统追求的是**认知异质性**，不是人数规模。允许冲突，鼓励少数派。一致性不是目标，发现结构性分歧才是价值来源。

### 2.2 信息源分离
| 系统 | 信息域 | 认知视角 |
|------|--------|----------|
| System A（政策高度） | 政策、监管、叙事、事件 | 政策分析师 / 宏观策略师 |
| System B（量价数值） | 量价、强弱、节奏、波动 | 量化分析师 / 技术派 |
| System C（对抗审计） | 市场结构、流动性、盘口深度、资金集中度 | 风险官 / 做空者 / 法医审计 |

### 2.3 十五角色全映射（博弈终版）

**System A — Policy-Centric（5 角色）**
| 角色 | 模型 | 类型 | 职责 |
|------|------|------|------|
| A-1 数据采集官 | 代码 + Gemini Flash | 代码+LLM | Tushare 数据抓取 + 新闻分类标注 |
| A-2 舆情管理官 | DeepSeek-V4 | 纯LLM | 交叉比对，输出结构化信号 |
| A-3 多头分析官 | GPT-5 | 纯LLM | 强多头视角，叙事构建 |
| A-4 战略承接官 | Gemini 2.5 Pro | 纯LLM | 政策-行业映射，叙事验证 |
| CDO-A 首席决策官 | Claude 4.6 Sonnet | 纯LLM | 保守裁判，制衡乐观团队 |

**System B — Quant-Centric（5 角色）**
| 角色 | 模型 | 类型 | 职责 |
|------|------|------|------|
| B-1 数据采集官 | 纯代码 | 代码 | Tushare 行情数据采集，零 LLM |
| B-2 趋势分析官 | Gemini 2.5 Pro | 纯LLM | 趋势 / 动量 / 形态判断 |
| B-3 量化分析官 | 代码引擎 + Claude Sonnet | 代码+LLM | 代码算指标，LLM 解读组合信号 |
| B-4 空头分析官 | DeepSeek-R1 | 纯LLM | 逻辑推理找量价破绽 |
| CDO-B 首席决策官 | GPT-5 (Thinking) | 纯LLM | 综合裁决，和下属全不同厂商 |

**System C — Adversarial-Audit（5 角色）**
| 角色 | 模型 | 类型 | 职责 |
|------|------|------|------|
| C-1 数据探查官 | 代码 + Gemini Flash | 代码+LLM | 龙虎榜/融资融券/股东数据 + 异常标注 |
| C-2 行政观测官 | DeepSeek-V4 | 纯LLM | 监管/大股东/合规风控 |
| C-3 反对派观察者 | DeepSeek-R1 | 纯LLM | 结构性漏洞，强制反向证据 |
| C-4 压力测试官 | 历史引擎 + GPT-5 (Thinking) | 代码+LLM | 历史相似统计 + 场景解读 |
| CDO-C 首席审计官 | Gemini 2.5 Pro (Thinking) | 纯LLM | 中性法官，证据一致性裁决 |

**CDO 厂商分布**：Claude(A) / GPT-5T(B) / GeminiProT(C) = 三家三风格

### 2.4 角色分类（确定性 vs 不确定性）
| 类型 | 角色 | 说明 |
|------|------|------|
| 纯代码 | B-1 | 零 LLM 消耗 |
| 代码+LLM | A-1, B-3, C-1, C-4 | 代码算数据/指标，LLM 做标注/解读 |
| 纯 LLM | 其余 10 角色 | 不确定性判断，需要推理和博弈 |

### 2.5 独立性原则（最高优先级）
- 三系统在产出结论前**完全隔离**，不可见其他系统的中间结果
- 执行流程：并行独立分析 → 各自提交密封结论 → 同时开封 → 进入辩论
- 禁止"先听到别人意见再进行高质量附和"
- B-4（空头分析官）和 C-3（反对派观察者）虽同为 DeepSeek-R1，但信息域严格隔离：
  - B-4 只看量价/形态/拥挤度，禁止引用板块联动/监管数据
  - C-3 只看结构/监管/板块/陷阱，禁止引用量价指标

### 2.6 数据时间戳一致性
- 每个角色输出必须携带 `as_of_time` 和 `data_staleness_sec`
- staleness 阈值按数据域分层（见 `config/models.yaml`）：
  - market_data：warning 3min / degraded 5min
  - news_event：warning 10min / degraded 20min
  - regulatory：warning 30min / degraded 60min
  - shareholder：warning 60min / degraded 120min
- MetaJudge 共识前校验：偏差 > 5min 降权最旧系统；全部 degraded 弃权

---

## 第 3 章 共识协议

### 3.1 三级异议体系

**softObjection**：记录存档，不影响动作。

**hardObjection**：降为试探仓 + 禁止隔夜 + 延迟确认。
- 判定模板（结构不可省略，阈值模拟盘校准）：
  - 龙头炸板：开板次数 >= 2 或 回封失败 + 成交额异常放大（至少满足两项）
  - 流动性陷阱：盘口承接衰减 + 成交回落 + 接近跌停（组合条件）
  - 历史失败案例：必须基于标签匹配，不接受自由类比
- CDO-C 去保守后补充硬规则：`false_prosperity=true` 时 hard 不可降为 soft

**vetoObjection**：直接禁止该笔交易（一票否决）。
- 穷举条件同前（监管/退市/龙头跌停/跌停回封失败）
- 例外桶（受审计）规则同前

### 3.2 Devil's Advocate 强制化
System C 报告必须包含 `anti_thesis_evidence`（>= 2 条）。为空或敷衍 → 中央仲裁官驳回本轮决策。

### 3.3 事件双确认与衰减
- 叙事确认 + 市场确认，衰减规则同前

### 3.4 状态相关共识门槛
| MSI 状态 | 开仓门槛 | 持仓管理 | 首仓比例 |
|----------|----------|----------|----------|
| 强攻 | 2/3 通过 | 正常持有 + 允许加仓 | 计划仓位 1/3 |
| 观望 | 3/3 一致 | 禁止新开仓 + 加仓，止盈收紧 | 极小 |
| 防守 | 禁止开仓 | 只减不加，止损收紧 | 0 |

CGI 附加约束同前。

### 3.5 策略分流共识
- 策略一（Trinity Alpha Momentum）：完整三系统共识
- 策略二（Volatility Compression Breakout）：仅需 B + C 同意，A 保留 veto 权

### 3.6 data_health 与弃权机制
- 每个系统输出 `data_health`：healthy / degraded / insufficient
- `insufficient + can_conclude=false` → 该系统标记为弃权，不计入共识
- `degraded` → confidence 降一档
- **数据不足 ≠ 利空**，不允许把数据缺失当作否定证据
- 弃权率监控：某系统长期高弃权率 → 修数据链路而非接受失衡

### 3.7 confidence 三档化
- 所有角色输出 confidence 改为三档：low / medium / high
- 禁止任何角色输出确定性 > 0.9
- MetaJudge 不做跨角色 confidence 横向比较，只看 signal + objection
- 校准曲线在积累 30 交易日数据后建立

### 3.8 紧急降级
- 触发条件：三系统辩论超过 60 秒未达共识
- 默认降级优先级：B > A > C
- 紧急模式下仓位上限减半

---

## 第 4 章 漏斗触发架构

### 4.1 L0 — 确定性硬过滤（Zero Cost）
- 执行者：Python 脚本，本地 CPU，零 API 消耗
- 数据源：Tushare `pro.daily` + `pro.stock_basic`
- 筛选条件：涨幅 > 2% / 换手率 > 1% / 非 ST / 成交额前 500
- 行业匹配：基于申万分类

### 4.2 L1 — 轻链路筛选（Low Cost）
- 只跑低成本规则或少量轻模型（Flash 级）
- 行业图谱匹配 + K 线形态初审 + LRI 硬否决
- 收窄至 3-5 只
- 大多数标的在此层被淘汰，不进入完整辩论

### 4.3 L2 — 重链路辩论（High Value）
- 仅前 3-5 名候选进入三系统 15 角色完整辩论
- 深度推理成本可控
- 目的不是省钱，而是把最强算力集中在最值得辩论的标的

---

## 第 5 章 交易策略与铁律

### 5.1 策略一：Trinity Alpha Momentum
**进场条件**同前。**出场逻辑**同前。

### 5.2 策略二：Volatility Compression Breakout
波动率压缩 → 突破型策略（辅助策略），规则同前。

### 5.3 T+1 铁律
偏好尾盘 14:30 后建仓。硬止损 -5%（不可协商）。

### 5.4 试探仓制度
- 首仓永远是计划仓位的 1/3
- 共识不等于安全：即使三方一致也先试探仓
- **运行上下文传递**：关键角色（B-2/B-4/C-3/各CDO）在调用时接收 `runtime_context`（task_type/strategy/position_state/overnight_allowed/current_phase），不写入角色 MD

### 5.5 交易铁律
聚焦 / 只做主线 / 只做龙头 / 不抄底 / 不买杂毛

---

## 第 6 章 三大核心指数

内容同前（MSI/CGI/LRI/三时段状态机）。

### 6.1 层级漏斗结构
```
第一层 MSI（市场许可）→ 今天能不能打？
第二层 CGI（博弈判断）→ 该打什么风格？
第三层 LRI（标的否决）→ 这只票能不能碰？
```

### 6.2 MSI — 市场强度指数
输入：量能 + 赚钱效应 + 指数位置。输出：强攻 / 观望 / 防守。
双重角色：进场漏斗 + 持仓风格切换器。

### 6.3 CGI — 资金博弈指数
四种博弈态：一致进攻 / 独角龙 / 补涨末端 / 拥挤陷阱。

### 6.4 LRI — 流动性风险指数
硬性二元否决。影子指标：仓位冲击比。

### 6.5 三时段状态机
| 时段 | 任务 | 约束 |
|------|------|------|
| 9:25-9:35 | 紧急熔断检查 | **只能产生熔断信号** |
| 10:10-10:20 | 主状态定性 | 决定今日进攻权限 |
| 13:30-14:00 | 尾盘再确认 | 确认强势是否兑现 |

---

## 第 7 章 运营机制

### 7.1 每日 SOP
1. 盘前：更新复盘结果 → 读取已批准策略版本 → 生成候选池
2. 9:25-9:35：异常熔断检查
3. 10:15：主状态判断 → 启动 AI 多代理博弈选股
4. 13:30-14:00：尾盘复核
5. 收盘后：自动复盘报告 → 用户极简日报 → 候选规则学习

### 7.2 自我修正（5 日窗口 + Context Snapshot）
每轮决策打包 Context Pack。收盘后 System C 总结误判。修正先进入候选池。

### 7.3 候选修正验证
每条候选变更必须通过：胜率不降 / 期望值 >= 5% / 回撤不增 / 少数派频率不降。

### 7.4 机会成本监控（四条件事前标准）
**missedOpportunity** 只统计同时满足以下四条件的标的：
1. 事前进入过候选池（L1 筛选后）
2. 当时满足可交易条件（LRI 通过，非涨停/停牌）
3. 被明确门槛或异议挡掉（有记录）
4. T+1 至 T+5 窗口内达到 +5% 以上涨幅

不满足四条件的"事后上涨"不计入。防止事后诸葛化。

- overBlockedTrade / defensiveDrift 规则同前
- 期望值 = 胜率 x 平均盈利 - 败率 x 平均亏损

### 7.5 收益质量监控（五维度拆解）
盘后复盘按以下 5 维度分别统计盈亏：
1. MSI 状态（强攻/观望）
2. CGI 博弈态
3. 策略类型（策略一/策略二）
4. 异议情况（有 hard 异议/无异议）
5. 试探仓验证（经过试探仓/直接仓位）

盈利连续 2 周集中在单一维度 → 触发"收益脆弱性警告"。

### 7.6 进化反同化纪律
- 每个系统只修正自己框架内的盲区，禁止跨系统学习
- System A 复盘只问叙事对不对，B 只问量价准不准，C 只问陷阱识别对不对

### 7.7 分场景相关性监控
- 按 CGI 状态分别统计三系统结论一致率
- 错误来源分布相似度 > 70% → 错误趋同警告
- **双 R1 影子对照**：每周抽样 5 次，用跨厂商 challenger 模型做影子复核
  - r1_r1_cross_agreement_rate > 70% 连续 2 周 → 必须评估换厂商

### 7.8 CDO 健康度监控（三指标联立）
橡皮章预警触发条件（必须同时满足）：
1. internal_dissent 出现率 > 30%（下属有分歧）
2. CDO override_rate = 0%（CDO 从未推翻任何一方）
3. CDO anti_thesis 文本相似度 > 80%（模板化）
持续 2 周 → 触发 CDO 审计。

### 7.9 复盘到交易三阶段转化协议
| 阶段 | 时机 | 规则 |
|------|------|------|
| 记录 | 即时 | 所有复盘结论存档，不影响盘中 |
| 候选 | 次日盘后 | 同类错误 5 日内 >= 2 次 → 升级为候选修正 |
| 晋升 | 周末验证 | 回测通过 + 每周最多 2 条进入实战 |

**critical_fix 快速通道**：单次亏损 >= 2% 且属于规则缺失 → 允许 1 次直接进入候选验证，但仍需周末验证。每月最多 1 条。

**候选修正分类（fix_type）**：
- `prompt`：修改角色 MD → 需 A/B 测试
- `rule`：修改 MetaJudge/风控规则 → 需回测验证
- `engine`：修改代码引擎参数 → 需参数敏感性分析
- `pipeline`：修改数据采集链路 → 需数据质量验证

### 7.10 协议健康度监控
| 指标 | 预警阈值 |
|------|---------|
| block_rate（L2 候选被 REJECT 比率）| > 90% 连续 2 周 |
| avg_decision_latency | > 45 秒 |
| decision_path_length | 持续增长 |
| rule_conflict_count | > 0 |

预警触发 → 审查是否有规则可合并或前移到轻链路。

### 7.11 角色能力画像（季度）
命中率 / 前瞻性 / 少数派价值 / 环境适配度 / 失误模式。

### 7.12 代码引擎版本化
所有代码计算引擎输出必须携带：version / parameter_set / sample_size / applicability_warning。

### 7.13 券商适配与执行
Broker Adapter 模式。盘中执行层禁止自我改写。

---

## 附录 硬风控清单（AI 不可覆盖）

以下约束为系统宪法，任何 AI Agent 均无权修改或绕过：

1. **单票仓位上限**：不超过总资金 20%
2. **单日最大亏损**：不超过总资金 3%，触发后当日停止开新仓
3. **连亏停机**：连续 3 个交易日亏损 → 自动停机 1 天
4. **T+1 约束**：当日买入不可当日卖出
5. **涨跌停特殊处理**：封死涨停时禁止卖出信号；接近跌停时 LRI 强制否决
6. **停牌/退市预警**：触发 vetoObjection
7. **紧急降级不可绕过**：60 秒超时必须降级
8. **禁止盘中自改逻辑**：执行层只使用已批准版本
9. **订单去重与超时保护**：Webhook 去重、重复下单保护、券商 API 失败重试与撤单
10. **杠杆与融资**：禁止
11. **vetoObjection 穷举条件**：见第 3 章，AI 不可自行添加新的 veto 条件
12. **LRI 硬否决**：流动性不足直接排除，不可协商
13. **MetaJudge 禁止调用 LLM**：中央仲裁官只执行共识协议，不解释事实，不重写结论，不创造新理由
14. **confidence 不做跨角色横向比较**：MetaJudge 只看 signal + objection 做决策
15. **复盘不直接改盘中**：所有修正必须经过三阶段转化协议（7.9 节）
